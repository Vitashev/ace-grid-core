# Ace Grid Public Core Export

This tree is generated from the private `gridix` workspace by:

```bash
npm run export:public-core
```

It is intended as the source snapshot for the public Core repository. Paid tier
entrypoints, paid tier runtime modules, portal, infra, and schema packages are
not copied into this export.

The private repository remains the source of truth for Pro and Enterprise npm
publishing.
